import {gallery} from '@/lib/gallery';
export default function MediaGallery(){if(!gallery.length)return null;return <section className="media-gallery" aria-labelledby="gallery-heading"><div className="section-heading"><h2 id="gallery-heading">In the lab.</h2></div><div className="gallery-grid">{gallery.map(item=><figure key={item.src}>{item.kind==='video'?<video controls playsInline preload="metadata" poster={item.poster} aria-label={item.title}><source src={item.src}/>{item.captions&&<track kind="captions" src={item.captions} srcLang="en" label="English" default/>}</video>:<img src={item.src} alt={item.alt} loading="lazy"/>}<figcaption>{item.title}</figcaption></figure>)}</div></section>}
